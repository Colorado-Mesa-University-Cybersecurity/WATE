from setuptools import setup, find_packages

setup(
    name='DeepTreeEnsemble',
    version='0.1',
    packages=find_packages(),
    description='Provides a framework for a new ensembling technique called a DeepTreeEnsemble',
    author='Riley Primeau',
    author_email='primeauriley@gmail.com',
    url='https://github.com/PrimoRiley',
    install_requires=[
        # Any dependencies you need, e.g., 'numpy>=1.18.1'
    ],
)
